#include<stdio.h>
/**
 * main - entry point
 *
 * Return:Always 0(success)
 */
int main(void)
{
	for (char c = 'a'; c <= 'z'; c++)
	{
		if (c != 'e' && c!='q')
			putcher("c");
	}
	putcher('\n');

	Return (0);
}
